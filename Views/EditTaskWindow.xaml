<Window x:Class="Pet.TaskDevourer.Views.EditTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="600" Width="600"
        MinHeight="450" MinWidth="450"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen">


    <Grid Margin="20"  MouseLeftButtonDown="Grid_MouseLeftButtonDown">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Some Text" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBox x:Name="TitleTextBox" Grid.Row="1" FontSize="14" Padding="8" Margin="0,0,0,15"/>


        <TextBlock Grid.Row="2" Text="Description" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <TextBox x:Name="DescriptionTextBox" Grid.Row="3" FontSize="14" Padding="8"
                 TextWrapping="Wrap" AcceptsReturn="True" Height="100" Margin="0,0,0,15"/>


        <TextBlock Grid.Row="4" Text="Due time" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <DatePicker x:Name="DueDatePicker" Grid.Row="5" FontSize="14" Margin="0,0,0,15" VerticalAlignment="Top"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SaveButton" Content="Save" Padding="15,8" Margin="0,0,10,0"
                    Background="#4CAF50" Foreground="White" BorderThickness="0"
                    Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Padding="15,8"
                    Background="#f44336" Foreground="White" BorderThickness="0"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>